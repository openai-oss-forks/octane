diff --git a/tests/upstream/useQuery.test.tsx b/tests/upstream/useQuery.test.tsx
--- a/tests/upstream/useQuery.test.tsx
+++ b/tests/upstream/useQuery.test.tsx
@@ -1,3 +1,4 @@
+import { timeoutManager } from '@tanstack/query-core'
 import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
 import { act, fireEvent, render } from '@octanejs/testing-library'
 import * as React from 'octane'
@@ -3939,7 +3940,9 @@ describe('useQuery', () => {
 
     await vi.advanceTimersByTimeAsync(0)
     rendered.getByText('fetched data')
-    const setTimeoutSpy = vi.spyOn(globalThis.window, 'setTimeout')
+    // OCTANE DIVERGENCE[native-query-test-harness]: observe query garbage-collection timers independently
+    // of Octane's own scheduling timers.
+    const setTimeoutSpy = vi.spyOn(timeoutManager, 'setTimeout')
 
     rendered.unmount()
 
@@ -3963,7 +3966,9 @@ describe('useQuery', () => {
     await vi.advanceTimersByTimeAsync(0)
     rendered.getByText('fetched data')
 
-    const setTimeoutSpy = vi.spyOn(globalThis.window, 'setTimeout')
+    // OCTANE DIVERGENCE[native-query-test-harness]: observe query garbage-collection timers independently
+    // of Octane's own scheduling timers.
+    const setTimeoutSpy = vi.spyOn(timeoutManager, 'setTimeout')
 
     rendered.unmount()
 
