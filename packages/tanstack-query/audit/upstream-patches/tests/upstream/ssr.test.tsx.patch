diff --git a/tests/upstream/ssr.test.tsx b/tests/upstream/ssr.test.tsx
--- a/tests/upstream/ssr.test.tsx
+++ b/tests/upstream/ssr.test.tsx
@@ -15,6 +15,7 @@ import {
 } from '../../src'
 import { setIsServer } from './utils'
 
+// OCTANE DIVERGENCE[native-query-test-harness]: server rendering exposes HTML through RenderResult.html.
 describe('Server Side Rendering', () => {
   setIsServer(true)
 
@@ -52,7 +53,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('status pending')
     expect(queryFn).toHaveBeenCalledTimes(0)
@@ -96,7 +97,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('status success')
     expect(queryFn).toHaveBeenCalledTimes(1)
@@ -163,7 +164,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('page 1')
     expect(queryFn).toHaveBeenCalledTimes(1)
@@ -192,7 +193,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('data')
     expect(markup).toContain('isFetching: 0')
@@ -230,7 +231,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('status1: success')
     expect(markup).toContain('status2: success')
@@ -251,7 +252,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('status: idle')
   })
@@ -267,7 +268,7 @@ describe('Server Side Rendering', () => {
       <QueryClientProvider client={queryClient}>
         <Page />
       </QueryClientProvider>,
-    )
+    ).html
 
     expect(markup).toContain('mutationState: 0')
   })
