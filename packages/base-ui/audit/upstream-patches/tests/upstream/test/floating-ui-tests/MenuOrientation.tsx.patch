diff --git a/tests/upstream/test/floating-ui-tests/MenuOrientation.tsx b/tests/upstream/test/floating-ui-tests/MenuOrientation.tsx
--- a/tests/upstream/test/floating-ui-tests/MenuOrientation.tsx
+++ b/tests/upstream/test/floating-ui-tests/MenuOrientation.tsx
@@ -59,20 +59,18 @@ const MenuContext = React.createContext<MenuContextType>({
 interface MenuProps {
   label: string;
   nested?: boolean;
-  children?: React.ReactNode;
+  children?: React.OctaneNode;
   keepMounted?: boolean;
   orientation?: 'vertical' | 'horizontal' | 'both';
   grid?: boolean;
 }
 
 /** @internal */
-export const MenuComponent = React.forwardRef<
-  HTMLButtonElement,
-  MenuProps & React.HTMLProps<HTMLButtonElement>
->(function Menu(
-  { children, label, keepMounted = false, grid, orientation: orientationOption, ...props },
-  forwardedRef,
+export const MenuComponent = (function Menu(
+  propsWithRef: (MenuProps & React.HTMLProps<HTMLButtonElement>) & { ref?: React.Ref<HTMLButtonElement> },
 ) {
+  const { ref: forwardedRef = null, ...propsWithRefRest } = propsWithRef;
+  const { children, label, keepMounted = false, grid, orientation: orientationOption, ...props } = propsWithRefRest;
   const [isOpen, setIsOpen] = React.useState(false);
   const [activeIndex, setActiveIndex] = React.useState<number | null>(null);
   const [allowHover, setAllowHover] = React.useState(false);
@@ -241,7 +239,7 @@ export const MenuComponent = React.forwardRef<
           </span>
         )}
       </button>
-      <MenuContext.Provider
+      <MenuContext
         // eslint-disable-next-line react/jsx-no-constructed-context-values
         value={{
           activeIndex,
@@ -297,7 +295,7 @@ export const MenuComponent = React.forwardRef<
             </FloatingPortal>
           )}
         </CompositeList>
-      </MenuContext.Provider>
+      </MenuContext>
     </FloatingNode>
   );
 });
@@ -308,10 +306,9 @@ interface MenuItemProps {
 }
 
 /** @internal */
-export const MenuItem = React.forwardRef<
-  HTMLButtonElement,
-  MenuItemProps & React.ButtonHTMLAttributes<HTMLButtonElement>
->(function MenuItem({ label, disabled, ...props }, forwardedRef) {
+export const MenuItem = (function MenuItem(propsWithRef: (MenuItemProps & React.ButtonHTMLAttributes<HTMLButtonElement>) & { ref?: React.Ref<HTMLButtonElement> }) {
+  const { ref: forwardedRef = null, ...propsWithRefRest } = propsWithRef;
+  const { label, disabled, ...props } = propsWithRefRest;
   const menu = React.useContext(MenuContext);
   const item = useCompositeListItem({ label: disabled ? null : label });
   const tree = useFloatingTree();
@@ -366,10 +363,8 @@ export const MenuItem = React.forwardRef<
 });
 
 /** @internal */
-export const Menu = React.forwardRef<
-  HTMLButtonElement,
-  MenuProps & React.HTMLProps<HTMLButtonElement>
->(function MenuWrapper(props, ref) {
+export const Menu = (function MenuWrapper(propsWithRef: (MenuProps & React.HTMLProps<HTMLButtonElement>) & { ref?: React.Ref<HTMLButtonElement> }) {
+  const { ref: ref = null, ...props } = propsWithRef;
   const parentId = useFloatingParentNodeId();
 
   if (parentId === null) {
